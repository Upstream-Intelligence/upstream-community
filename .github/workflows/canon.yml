name: canon

on:
  push:
  pull_request:

jobs:
  canon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: No legacy pack/specialty machinery
        run: bash scripts/check-canon.sh
